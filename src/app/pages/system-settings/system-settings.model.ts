export interface Code {
    code: number;
    name: string;
    eng: string;
    tableName: string;
}

export interface CodeTable {
    hasCenter: number
    hide: number
    serial: number
    sortTable: number
    tableName: string
    tableTitle: string
}